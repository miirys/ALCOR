# GitLab Duo Non-Agentic Chat

This package contains the non-agentic version of GitLab Duo Chat.

It is used by all IDEs that offer Non-Agentic Chat, except:

- VS Code WebIDE (does not use Language Server, so it uses the Chat copy that is directly in VS Code)
